# 大师看和弦「卡在15%」Bug 修复报告

## 根因分析

### 症状
页面上传音频文件后卡在「解码音频文件…15%」状态，永远不会前进。

### 根因：主线程 AudioContext 因 autoplay policy 死锁

Chrome/Chromium 的 autoplay policy 要求 AudioContext 必须在**用户手势**(user gesture)期间创建或 resume。当 `Upload.Dragger` 拖拽文件时，`beforeUpload` 回调虽然来自用户点击，但 `FileReader.readAsArrayBuffer` + `AudioContext` 的创建发生在异步链中，**此时 user activation 已丢失**。

关键特性：
- `new AudioContext()` 在无 user activation 时会被设为 `'suspended'` 状态
- `audioContext.resume()` 在无 user activation 时会**静默 await**（不抛错也不返回）
- `audioContext.decodeAudioData()` 也会在 suspend 状态下**静默卡住**

### 原始代码的问题链

```
1. 用户拖拽文件
2. beforeUpload 回调 → handleFileSelect
3. extractPeaks() → new AudioContext()  ← 此时 user gesture 已丢失
4. audioContext.resume()  ← 永远 await
5. audioContext.decodeAudioData()  ← 等 resume 完成，死锁
```

## 修复方案

### 核心思路：完全绕开主线程 AudioContext

将音频解码移到 **Web Worker** 中执行。Web Worker 中的 AudioContext 不受 autoplay policy 影响。

### 具体修改

1. **`src/hooks/useChordAnalysis.ts`** — 删除 `extractPeaks`，新增 `decodeInWorker`：
   - 不再在主线程创建 `AudioContext`
   - 将 raw ArrayBuffer 通过 Transferable 零拷贝传给 Worker
   - Worker 完成后返回 `{peaks, audioData, sampleRate, duration}`
   - 60 秒超时保护

2. **`src/services/chord/analysis.worker.ts`** — 新增 `handleDecode` 函数：
   - 在 Worker 中使用 `OfflineAudioContext.decodeAudioData` 解码
   - 同同时提取 2000 点归一化 peaks
   - 通过 Transferable 零拷贝返回数据

3. Worker 消息协议支持两种消息类型：
   - `{type: 'decode', rawBuffer}` → 返回 `{type: 'decodeResult', ...}`
   - `{type: 'analyze', audioBuffer, sampleRate}` → 返回 `{type: 'result', features}`

## 验证方法

1. **重启 dev server**（必须，因为 mako 缓存了旧代码）：
   ```bash
   pnpm start:dev
   ```

2. 打开 `http://localhost:8000/Suno-Cover-Arranger/`

3. 点击「大师看和弦」→ 拖拽或上传音频文件

4. 预期行为：进度条从 15% 推进到 100%，显示和弦分析结果

## 修改文件清单

| 文件 | 变更 | 说明 |
|------|------|------|
| `src/hooks/useChordAnalysis.ts` | 重写 | extractPeaks → decodeInWorker，完全移除主线程 AudioContext |
| `src/services/chord/analysis.worker.ts` | 新增 handleDecode | Worker 端音频解码+peaks提取 |
| `src/services/chord/index.ts` | 删除导出 | 移除 pipeline/cache 死代码导出 |
| `src/services/db.ts` | 删除表 | 移除 chordAnalysisCache 表 |
| `src/shared/types/types.ts` | 删除类型 | 移除 CachedAnalysis |
| `typings.d.ts` | 新增 | essentia.js 类型声明 |
| `src/pages/chord-analysis/components/WaveformCanvas.tsx` | 修 bug | 播放头三角形位置修复 |

已删除的死代码文件：
- ~~`src/services/chord/pipeline.ts`~~
- ~~`src/services/chord/pipeline.worker.ts`~~
- ~~`src/services/chord/cache.ts`~~
- ~~`src/services/chord/mockPipeline.ts`~~

## 构建状态

- `pnpm build` ✅ 通过（2.1s）
- TypeScript 检查 ✅ 仅 1 个预先存在的 `import.meta.url` 警告（UmiJS 正确处理）
