# 「大师看和弦」功能代码核查报告

> 核查人：Senior Developer (高级开发工程师)  
> 核查日期：2026-07-06  
> 项目：Suno-Cover-Arranger v2.2.0  

---

## 一、总体评估

「大师看和弦」是一个**技术选型合理、算法设计扎实**的功能。核心算法翻写自 Chordino（和弦匹配 + Viterbi 解码）和 chord-romanizer（级数映射），数学上是正确的。但存在**两个并行实现的代码腐败、一个运行时会崩溃的函数签名错误、以及若干内存泄漏和资源管理问题**。

**严重度分布：**
- 🔴 高危（会导致崩溃/数据错误）：3 项
- 🟡 中危（影响稳定性/性能）：5 项  
- 🟢 低危（代码质量/维护性）：6 项

---

## 二、核心架构问题

### 🔴 问题 1：两套并行实现，一套从未运行，一套无法运行

```
src/services/chord/
├── analysis.worker.ts    ← 旧版，实际在用（useChordAnalysis.ts 直接用它）
├── pipeline.ts           ← 新版流水线（纯函数，需要 inject extractFeaturesFn）
├── pipeline.worker.ts    ← 新版 Worker 入口（调用 pipeline.ts）
└── ...
```

**分析：**

- `useChordAnalysis.ts` 的 `startAnalysis`（第 359 行）直接创建 `analysis.worker.ts` Worker，绕开了 `pipeline.worker.ts`。
- `_createWorker`（第 268 行）能够创建 `pipeline.worker.ts` Worker，但**从未被 `startAnalysis` 调用**。
- 即便被调用，`pipeline.worker.ts` 第 203-209 行调用 `analyzePipeline` 时**只传了 5 个参数**，缺少第 6 个 `extractFeaturesFn`：

```typescript
// pipeline.worker.ts:203-209 — 缺少 extractFeaturesFn 参数！
const result: SongAnalysis = await analyzePipeline(
  audioBuffer,
  fileName,
  fileHash,
  fileSize,
  progressCallback,
  // ← 缺少 extractFeaturesFn
);
```

而 `pipeline.ts` 第 60-62 行会直接抛出异常：

```typescript
if (!extractFeaturesFn) {
  throw new Error('extractFeatures 未注入');
}
```

**结论：`pipeline.worker.ts` 是永远无法成功运行的死代码。**两个 Worker 之间各自实现了完全不同的和弦匹配逻辑（`analysis.worker.ts` 使用 5 帧中值滤波 + Beat 聚合，`pipeline.ts` 使用余弦相似度 + Viterbi 解码），但核心算法全在 `analysis.worker.ts` 中重复实现了一遍。

**建议：** 统一到底用哪一套方案，删除另一套。如果保留 pipeline 方案，需要修复参数传递并让 `useChordAnalysis` 转向调用 `pipeline.worker.ts`。

---

### 🟡 问题 2：缓存逻辑在 Hook 和 cache.ts 中重复实现

- `useChordAnalysis.ts` 第 133-178 行自己又实现了一套 `checkCache` / `saveToCache`，包含 LRU 淘汰（只删一条）和 TTL 检查。
- `cache.ts` 也实现了一套完整的缓存逻辑（TTL + LRU 淘汰 + 统计）。

两套逻辑中 TTL 都是 30 天、上限都是 50 条，但 Hook 里的 LRU 淘汰只删最旧的一条（`orderBy('createdAt').first()`），而 `cache.ts` 的 `evictLRU` 是一次性删除所有超量条目。**如果缓存插入后超过 51 条，Hook 的淘汰逻辑只删 1 条，导致缓存永远不会降到 50 条以下。**

---

## 三、运行时会崩溃的 Bug

### 🔴 问题 3：`saveToCache` 参数数量不匹配（TypeScript 类型错误未暴露）

**位置：** `useChordAnalysis.ts` 第 442 行

```typescript
// 函数签名：async (analysis: SongAnalysis)
const saveToCache = useCallback(async (analysis: SongAnalysis) => { ... }, []);

// 调用：传了 2 个参数，但只接收 1 个
saveToCache(fileHash, result);  // ← BUG!
```

`saveToCache` 内部使用 `analysis.fileHash`、`analysis.vocabularyLevel` 等属性来构建 `CachedAnalysis` 对象。但这里传的第一个参数是 `string`（fileHash），第二个是 `result`（SongAnalysis 对象）。函数内部会把 `fileHash` 字符串当作 `SongAnalysis` 类型使用：

```typescript
await db.chordAnalysisCache.put({
  fileHash: analysis.fileHash,    // string 无 .fileHash → undefined
  vocabularyLevel: analysis.vocabularyLevel,  // undefined
  ...
});
```

**后果：Dexie.js 以 `fileHash` 为主键，存入 `undefined` 会导致数据库写入失败（但被 try-catch 静默吞掉），缓存永远不会有效写入。**

TypeScript 在编译时应该能捕获这个错误，但代码中大量使用 `as any`（第 438 行），导致类型检查被绕过。

---

### 🔴 问题 4：缓存命中后状态闪烁（READY → DECODING → READY）

**位置：** `useChordAnalysis.ts` 第 327-344 行

```typescript
if (cached) {
  message.success('从缓存加载');
  setSongAnalysis(cached);
  setAnalysisStatus('READY');        // 1. 设为 READY
  ...
  setAnalysisStatus('DECODING');      // 2. 立即又设为 DECODING
  try {
    const { peaks: extractedPeaks } = await extractPeaks(file);
    setPeaks(extractedPeaks);
  } catch { ... }
  setAnalysisStatus('READY');        // 3. 再改回 READY
  return;
}
```

**后果：UI 会在 READY 和 DECODING 之间闪烁一次。用户在缓存命中时看到"解码音频文件…"的进度提示，但实际只是在提取峰值数据。** 如果峰值提取耗时较长（大文件），这个闪烁会更明显。

**建议：** 缓存命中时直接保持 READY 状态，峰值提取用独立的状态标记，或在后台静默完成。

---

## 四、开源库调用问题

### 🟡 问题 5：essentia.js WASM 加载使用 `eval()`（安全风险 + 脆弱的依赖路径）

**位置：** `pipeline.worker.ts` 第 282-300 行

```typescript
const wasmResponse = await fetch(wasmScriptUrl);
const wasmScript = await wasmResponse.text();
eval(wasmScript);  // ← 安全风险：执行远程 JS 代码
```

- `eval()` 直接执行从网络拉取的脚本，这是一个严重的安全隐患。如果 CDN 被劫持或供应链攻击，攻击者可以在 Worker 中执行任意代码。
- 路径硬编码了部署环境：开发环境用 `/essentia-wasm/`，生产用 `/Suno-Cover-Arranger/essentia-wasm/`。这在不同的部署场景（如本地 `file://` 协议、自定义域名、子路径部署）下会直接 404。
- `analysis.worker.ts` 使用 `import` 语句加载 `essentia.js/dist/essentia.js-core.es.js`，这是更正确的方式，但依赖 `essentia.js` 版本中的内部路径（可能在新版本中改变）。

**建议：** 
1. 删除 `eval()` 方案，统一使用 `import` 方式。
2. 将 WASM 资源路径作为配置项或通过构建工具（如 Vite/Rspack）的 public path 来确定，而非硬编码。

---

### 🟡 问题 6：essentia.js KeyExtractor 参数硬编码导致兼容性差

**位置：** `analysis.worker.ts` 第 22 行

```typescript
const keyResult = essentia.KeyExtractor(
  signalVec, true, 4096, 4096, 12, 3500, 60, 25, 0.2,
  'temperley', sr, 0.0001, 440, 'cosine', 'hann'
);
```

- 14 个位置的参数全硬编码，没有任何解释性注释。参数含义不透明，后续维护者无法判断是否合理。
- `profileType: 'temperley'` 和 `hpcpSize: 12` 是合理的选择（对应音乐理论中的 12 音调体系）。
- `maxFrequency: 3500` 和 `minFrequency: 60` 的选取理由不明——和弦识别通常关注中低频，这个范围是合理的。

**建议：** 添加参数含义注释，或将关键参数提取为命名常量。

---

### 🟡 问题 7：HPCP 帧提取失败时静默跳过，可能导致分析结果不完整

**位置：** `analysis.worker.ts` 第 42-53 行

```typescript
for (let i = 0; i < numFrames; i++) {
  try {
    const w = essentia.Windowing(fv, true, frameSize, 'hann', 0, true);
    const sp = essentia.Spectrum(w.frame, frameSize);
    const p = essentia.SpectralPeaks(sp.spectrum, 0, 5000, 100, 40, 'frequency', sr);
    if ((p.frequencies as any).size() < 1) continue;  // ← 静默跳过
    const h = essentia.HPCP(...);
    chromaFrames.push(essentia.vectorToArray(h.hpcp) as Float32Array);
  } catch { peakErr++; }  // ← 静默吞掉异常
}
```

- 帧提取失败只会增加 `peakErr` 计数器，但不会向用户报告。如果大量帧失败，用户看到的是不完整的分析结果，感知不到问题。
- 丢失帧意味着时间线断裂——和弦段落时间不连续，但后续处理（如 chordSegments 组装）假设帧是连续的。

---

## 五、业务逻辑问题

### 🟡 问题 8：Viterbi 解码的转移矩阵构建每次调用都重新计算

**位置：** `viterbi.ts` 第 195-206 行

```typescript
export function decodeChordSequence(observations, level = 'extended') {
  const templates = generateTemplates(level);       // 生成 36+1 模板
  const chordNames = templates.map((t) => t.name);  // 提取名称
  const transitionMatrix = buildTransitionMatrix(chordNames);  // 构建 37×37 矩阵
  return viterbiDecode(observations, chordNames, transitionMatrix);
}
```

- 每次分析都会生成相同的模板和相同的转移矩阵。对于 extended 级别，转移矩阵是 37×37 = 1369 个元素的二维数组，每次都重新计算。
- `analysis.worker.ts` 中**根本没有调用 Viterbi 解码**，它使用的是 5 帧中值滤波 + Beat 聚合。而 `pipeline.ts` 使用 Viterbi 但当前不可用。

**建议：** 如果最终选择 Viterbi 方案，将模板和转移矩阵缓存到模块级变量，按 `level` 参数做记忆化。

---

### 🟡 问题 9：Key/Scale 字符串拼接可能格式不一致

**位置：** `pipeline.ts` 第 134 行 vs `useChordAnalysis.ts` 第 429 行

```typescript
// pipeline.ts — 首字母大写 + 其余不变
key: `${keyRoot} ${features.scale.charAt(0).toUpperCase()}${features.scale.slice(1)}`
// 结果：'C Major'

// useChordAnalysis.ts — 直接拼接（小写 scale）
key: `${features.key} ${features.scale}`
// 结果：'C major'
```

分析结果中 Key 格式不一致，一个 `'C Major'`，一个 `'C major'`。虽然对用户显示影响不大，但如果后续有基于 Key 字符串做比较的逻辑（如缓存查找、搜索），会因大小写不匹配导致问题。

---

### 🟢 问题 10：BPM 中位数计算在极端情况下不鲁棒

**位置：** `analysis.worker.ts` 第 28-33 行

```typescript
let bpm = 120;
if (tickArr.length >= 2) {
  const iv: number[] = [];
  for (let i = 1; i < tickArr.length; i++) iv.push(tickArr[i] - tickArr[i - 1]);
  iv.sort((a, b) => a - b);
  bpm = Math.round(60 / iv[Math.floor(iv.length / 2)]);
}
```

- 如果 `iv` 的中位数是 0（两个连续的 tick 在同一位置），会导致 `60 / 0 = Infinity`，`Math.round(Infinity)` = `Infinity`。
- 在较快 BPM 的音乐中（>200 BPM），中位数可能接近 0.25-0.3 秒，这是正常的。但 `BeatTrackerMultiFeature` 的参数 `(signalVec, 208, 40)` 中 208 是最大 BPM，在这个范围内是安全的。

**建议：** 对中位数做下限保护：`iv[Math.floor(iv.length / 2)] || 0.5`，确保不会除以 0。

---

### 🟢 问题 11：WaveformCanvas 中播放头三角形指示器可能绘制到 Canvas 外

**位置：** `WaveformCanvas.tsx` 第 299-306 行

```typescript
// 顶部三角形指示器
ctx.fillStyle = PLAYHEAD_COLOR;
ctx.beginPath();
ctx.moveTo(phX, 0);
ctx.lineTo(phX - 5, -6);  // y = -6，在 Canvas 可视区域之外
ctx.lineTo(phX + 5, -6);  // 同上
ctx.closePath();
ctx.fill();
```

三角形顶点在 y = -6，而 Canvas 的可见区域从 y = 0 开始。Canvas API 对此行为是裁剪还是渲染到负坐标取决于浏览器实现。在大多数浏览器中，这部分会被裁剪掉，导致三角形不可见。

**建议：** 将三角形改为向下绘制（在 Canvas 顶部内侧），或增加 Canvas 顶部 padding。

---

### 🟢 问题 12：chord-templates.ts 中音符命名与 romanizer 模块不一致

**chord-templates.ts (NOTES):**
```typescript
['C', 'C#', 'D', 'Eb', 'E', 'F', 'F#', 'G', 'Ab', 'A', 'Bb', 'B']
//          ↑ 降号      ↑ 降号    ↑ 降号
```

**chord-parser.ts (NOTE_NAMES):**
```typescript
['C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B']
//          ↑ 升号      ↑ 升号    ↑ 升号
```

- 和弦模板产生的名字如 `Eb`, `Ab`, `Bb`，而 chord-parser 用 `D#`, `G#`, `A#`。
- 当 `pipeline.ts` 将模板匹配的和弦名传给 Romanizer 解析时，`parseChord('Eb')` 会正常因为 `normalizeNotePc` 使用 `SEMITONE_MAP`（包含两种拼写）而正常运行。
- 但 `degreeFromSpelling` 中按 `NOTE_LETTERS` 做音程计算时，`Eb` 的字母是 `E`，`Ab` 的字母是 `A`，这些都是在 `NOTE_LETTERS` 中的，所以计算结果是正确的。
- **结论：当前不会导致错误，但命名不一致增加了维护者的心智负担。** 建议统一使用一种拼写体系。

---

## 六、资源管理问题

### 🔴 问题 13：`analysis.worker.ts` Worker 不随组件卸载而终止

**位置：** `useChordAnalysis.ts` 第 359-401 行

```typescript
const w = new Worker(
  new URL('../services/chord/analysis.worker.ts', import.meta.url),
  { type: 'module' },
);
// ... 
w.onmessage = (e) => {
  if (e.data.type === 'error' || e.data.type === 'result') {
    clearTimeout(timeout);
    w.terminate();  // ← 只有收到消息后才终止
  }
};
```

- 如果用户在分析过程中离开页面（卸载组件），`analysis.worker.ts` Worker **不会被终止**。组件卸载时的清理逻辑（第 570-578 行）只处理了 `pipeline.worker.ts`：

```typescript
useEffect(() => {
  return () => {
    terminateWorker();     // 只终止了 pipeline.worker.ts
    clearTimeout_();
    if (audioUrl) URL.revokeObjectURL(audioUrl);
  };
}, [terminateWorker, clearTimeout_, audioUrl]);
```

- `analysis.worker.ts` 在 9 万秒超时前会一直运行，占用内存和 CPU，且 Essentia WASM 实例不会被释放。

**建议：** 将 `analysis.worker.ts` 的 Worker 实例存入 ref，在组件卸载时也终止它。

---

### 🟡 问题 14：AudioContext 未在所有路径上关闭

**位置：** `useChordAnalysis.ts` 第 236 行

```typescript
const audioContext = new AudioContext();
const audioBuffer = await audioContext.decodeAudioData(arrayBuffer);
// ... 提取峰值 ...
await audioContext.close();  // 正常路径关闭了

// 但如果 decodeAudioData 抛出异常，audioContext 不会被关闭！
```

`decodeAudioData` 失败时，`audioContext` 泄漏。`extractPeaks` 函数被 `startAnalysis` 中的 try-catch 包裹，所以异常被捕获但 audioContext 未关闭。

---

## 七、代码质量与规范问题

### 🟢 问题 15：多处 `as any` 绕过类型检查

- `useChordAnalysis.ts` 第 438 行：`} as any;` — 绕过了 TypeScript 对 `SongAnalysis` 的类型检查
- `analysis.worker.ts` 第 49 行：`(p.frequencies as any).size()` — essentia.js 类型定义不完整，但可以用 `(p.frequencies as Float32Array).length` 替代
- `pipeline.worker.ts` 第 289-302 行：多处 `(self as any)` — Worker 全局变量类型不完整

### 🟢 问题 16：biome-ignore 注释禁用了无障碍检查

**位置：** `chord-analysis/index.tsx` 第 113 行

```typescript
{/* biome-ignore lint/a11y/useMediaCaption: 用户上传的音频无需字幕 */}
<audio ref={audioRef} src={audioUrl} preload="auto" aria-label="音频播放" />
```

虽然没有字幕是合理的（用户上传的音频），但 `aria-label` 已经提供了基础可访问性，`biome-ignore` 注释在 `eslint-disable-next-line` 之外又多了一层，说明 lint 工具配置可能冲突。

---

## 八、总结与优先级修复建议

| 优先级 | 问题 | 修复方案 |
|--------|------|----------|
| **P0** | 问题 3：`saveToCache` 参数错误 | 改为 `saveToCache(result)` |
| **P0** | 问题 1：两套并行实现 | 选择一套，删除另一套 |
| **P0** | 问题 4：缓存命中状态闪烁 | 缓存命中时不切换状态 |
| **P1** | 问题 13：Worker 不终止 | ref 存储 worker 实例并在卸载时终止 |
| **P1** | 问题 5：eval() WASM 加载 | 改用 import 方式 |
| **P1** | 问题 14：AudioContext 泄漏 | 加 try-finally 确保 close() |
| **P2** | 问题 9：Key 格式不一致 | 统一使用首字母大写 |
| **P2** | 问题 10：BPM 除零保护 | 加下限保护 |
| **P2** | 问题 2：缓存逻辑重复 | 统一使用 cache.ts |
| **P3** | 问题 8：转移矩阵缓存 | 模块级缓存 |
| **P3** | 问题 12：音符命名统一 | 文档化或重构 |
| **P3** | 问题 11：播放头三角形 | 调整 Y 坐标 |

---

**总结：** 核心算法（和弦模板匹配 + Viterbi 解码 + 级数映射）翻写质量较高，数学逻辑正确。主要问题是工程层面的腐败——两个 Worker 并行、缓存逻辑重复、类型安全被 `as any` 绕过导致隐藏 bug。建议优先修复 P0 的三个问题，然后统一管线架构。

