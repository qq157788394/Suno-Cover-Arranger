/**
 * 这个文件作为组件的目录
 * 目的是统一管理对外输出的组件，方便分类
 */

import ConfirmDialog from './ConfirmDialog';
import ProTableWrapper from './ProTableWrapper';
import ResultCard from './ResultCard';

export { ProTableWrapper, ConfirmDialog, ResultCard };
